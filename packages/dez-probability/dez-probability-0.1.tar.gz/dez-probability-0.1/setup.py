from setuptools import setup

setup(name='dez-probability',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['dez_probability'],
      author='Jacob Sherwood',
      zip_safe=False)
