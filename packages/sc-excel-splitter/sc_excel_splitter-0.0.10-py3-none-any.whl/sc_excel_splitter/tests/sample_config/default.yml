excel:
  # 源文件路径
  source_file_path: "广州分行考核指标统计.xlsx"
  # 导出文件的保存路径
  target_directory: "./"
  # 需要按列拆分的Sheet以及拆分所在列
  sheet_config:
    # 需要拆分的Sheet名称，用双引号包围
    "客户经理汇总":
      # 拆分列所在列的索引（从0开始计数，第一列为0，依此类推）
      split_column: 2
      # 表头所在行的索引（从0开始计数，第一列为0，依此类推）
      header: 0
    # 需要拆分的Sheet名称，用双引号包围
    "机构汇总":
      # 拆分列所在列的索引（从0开始计数，第一列为0，依此类推）
      split_column: 0
      # 表头所在行的索引（从0开始计数，第一列为0，依此类推）
      header: 0
    # 需要拆分的Sheet名称，用双引号包围
    "理财":
      # 拆分列所在列的索引（从0开始计数，第一列为0，依此类推）
      split_column: 0
      # 表头所在行的索引（从0开始计数，第一列为0，依此类推）
      header: 0
    "贵金属":
      # 拆分列所在列的索引（从0开始计数，第一列为0，依此类推）
      split_column: 0
      # 表头所在行的索引（从0开始计数，第一列为0，依此类推）
      header: 0
    "存款":
      # 拆分列所在列的索引（从0开始计数，第一列为0，依此类推）
      split_column: 0
      # 表头所在行的索引（从0开始计数，第一列为0，依此类推）
      header: 0
    "代理保险业务明细表":
      # 拆分列所在列的索引（从0开始计数，第一列为0，依此类推）
      split_column: 0
      # 表头所在行的索引（从0开始计数，第一列为0，依此类推）
      header: 0
    "融卡":
      # 拆分列所在列的索引（从0开始计数，第一列为0，依此类推）
      split_column: 0
      # 表头所在行的索引（从0开始计数，第一列为0，依此类推）
      header: 0
    "转卡":
      # 拆分列所在列的索引（从0开始计数，第一列为0，依此类推）
      split_column: 0
      # 表头所在行的索引（从0开始计数，第一列为0，依此类推）
      header: 0
    "心卡":
      # 拆分列所在列的索引（从0开始计数，第一列为0，依此类推）
      split_column: 0
      # 表头所在行的索引（从0开始计数，第一列为0，依此类推）
      header: 0
    "易得贷":
      # 拆分列所在列的索引（从0开始计数，第一列为0，依此类推）
      split_column: 0
      # 表头所在行的索引（从0开始计数，第一列为0，依此类推）
      header: 0
    "分期余额":
      # 拆分列所在列的索引（从0开始计数，第一列为0，依此类推）
      split_column: 0
      # 表头所在行的索引（从0开始计数，第一列为0，依此类推）
      header: 0
    "线下+房抵贷+秒贷":
      # 拆分列所在列的索引（从0开始计数，第一列为0，依此类推）
      split_column: 0
      # 表头所在行的索引（从0开始计数，第一列为0，依此类推）
      header: 0
    "分期投放":
      # 拆分列所在列的索引（从0开始计数，第一列为0，依此类推）
      split_column: 0
      # 表头所在行的索引（从0开始计数，第一列为0，依此类推）
      header: 0
    "信用卡发卡量":
      # 拆分列所在列的索引（从0开始计数，第一列为0，依此类推）
      split_column: 0
      # 表头所在行的索引（从0开始计数，第一列为0，依此类推）
      header: 0
    "信用卡收入":
      # 拆分列所在列的索引（从0开始计数，第一列为0，依此类推）
      split_column: 0
      # 表头所在行的索引（从0开始计数，第一列为0，依此类推）
      header: 0
    "财富客户":
      # 拆分列所在列的索引（从0开始计数，第一列为0，依此类推）
      split_column: 0
      # 表头所在行的索引（从0开始计数，第一列为0，依此类推）
      header: 0
