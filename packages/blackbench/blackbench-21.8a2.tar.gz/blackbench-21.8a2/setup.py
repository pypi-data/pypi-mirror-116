#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['blackbench', 'blackbench.normal-targets.black']

package_data = \
{'': ['*'],
 'blackbench': ['micro-targets/*',
                'misc/*',
                'normal-targets/*',
                'normal-targets/flit/*',
                'normal-targets/flit_core/*',
                'task-templates/*']}

package_dir = \
{'': 'src'}

install_requires = \
['click >= 8.0.0', 'cloup >= 0.9.0', 'pyperf >= 2.0.0, <3']

entry_points = \
{'console_scripts': ['blackbench = blackbench:main']}

setup(name='blackbench',
      version='21.8a2',
      description='A benchmarking suite for Black, the Python code formatter.',
      author='Richard Si',
      author_email='63936253+ichard26@users.noreply.github.com',
      url='https://github.com/ichard26/blackbench',
      packages=packages,
      package_data=package_data,
      package_dir=package_dir,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.8',
     )
