[tool.poetry]
name = "python-sourcery"
version = "0.1.0"
description = "Sources is all you need."
authors = ["Amirouche <amirouche@hyper.dev>"]
license = "MIT"
readme = "README.md"
packages = [
    { include = "sourcery.py" },
]

[tool.poetry.dependencies]
python = "^3.7"
loguru = "^0.5.3"
httpx = "^0.18.2"
asyncio-foundationdb = "^0.10.1"
ujson = "^4.0.2"

[tool.poetry.dev-dependencies]
debug = "^0.3.2"
ipython = "^7.26.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
