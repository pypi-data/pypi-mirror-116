[tool.poetry]
name = "commend"
version = "0.1.0"
description = ""
authors = ["0x4448 <86135470+0x4448@users.noreply.github.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]
pytest = "^6.2.4"
black = "^21.7b0"
coverage = "^5.5"
flake8 = "^3.9.2"
pre-commit = "^2.13.0"

[tool.poetry.scripts]
commend = 'commend.cli:main'

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
