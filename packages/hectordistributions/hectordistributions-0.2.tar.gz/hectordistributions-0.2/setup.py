from setuptools import setup

setup(name='hectordistributions',
      version='0.2',
      description='Gaussian and Binomial distributions',
      packages=['hectordistributions'],
      zip_safe=False)
