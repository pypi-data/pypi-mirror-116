from setuptools import setup

setup(name='sbbbxl_distrib',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['sbbbxl_distrib'],
      author = 'Saiphan Bruner',
      zip_safe=False)
