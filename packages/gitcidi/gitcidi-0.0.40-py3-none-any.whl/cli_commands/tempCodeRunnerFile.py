            # path=pathlib.Path().resolve()
            # pathr=path+"/"+"cli-tool"
            # for root, subdirs, files in os.walk(pathr):
            #    for d in subdirs:
            #       if d == ".git":
            #        print("found the clone") 